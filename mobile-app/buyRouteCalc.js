// Smart buying route calculator: choose resource → build optimal path through vending machines
export function computeBuyRoute(resourceId, machines) {
  // nearest-neighbor path with price/stock weight
  return { route: [], totalCost: 0 };
}
