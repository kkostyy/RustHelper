// Panic mode: reset active tokens + clear history on specific gesture/combination
export function triggerPanic() { /* сброс токенов + очистка истории */ }
// Паник-режим: сброс токенов + очистка при жесте
